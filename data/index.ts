/**
 * Barrel export for the content layer.
 *
 * Components should import from `@/data` rather than reaching into individual
 * files, so content can be reorganised without touching every consumer.
 */

export * from "./profile";
export * from "./skills";
export * from "./content";
export * from "./projects";
